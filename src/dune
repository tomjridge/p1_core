; (include ../dune) automatically included?

(library
  (name p1_core)
  (public_name p1_core)
  (wrapped false)
  (flags (:standard -open Tjr_lib))
;  (preprocess (pps ppx_deriving_yojson))
  (libraries tjr_lib))

; (include_subdirs unqualified)
